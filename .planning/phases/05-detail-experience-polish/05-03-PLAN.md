---
phase: 05-detail-experience-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeModeSections.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeSettingsScreen.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt
  - app/src/main/java/com/peter/overtimecalculator/MainActivity.kt
  - app/src/androidTest/java/com/peter/overtimecalculator/MainFlowTest.kt
  - app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt
autonomous: true
requirements:
  - UXP-02
  - UXP-03
must_haves:
  truths:
    - All three theme mode choices are visible at once in settings without sideways scrolling
    - Theme mode still switches instantly through the existing settings callbacks
    - The settings top/status-bar area looks like part of the same settings surface language
    - Home screen styling does not regress while settings chrome is harmonized
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeModeSections.kt
      provides: Compact three-choice theme selector
    - path: app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt
      provides: Shared settings top-bar chrome used by all settings routes
    - path: app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt
      provides: Shared system-bar/theme coordination under edge-to-edge
    - path: app/src/androidTest/java/com/peter/overtimecalculator/MainFlowTest.kt
      provides: Theme-settings visibility and switching regression coverage
  key_links:
    - from: app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeModeSections.kt
      to: app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeSettingsScreen.kt
      via: existing `onThemeSelected` callback path
      pattern: compact control preserves `AppTheme` switching behavior and test tags
    - from: app/src/main/java/com/peter/overtimecalculator/MainActivity.kt
      to: app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt
      via: edge-to-edge + shared settings chrome/theme defaults
      pattern: status-bar/top-bar harmony is fixed centrally, not per screen
---

<objective>
Compact theme switching and harmonize settings chrome under edge-to-edge.

Purpose: make settings faster to operate and visually cleaner by removing oversized theme-mode cards and eliminating the mismatched settings status-bar strip.

Output: compact theme mode control, shared settings chrome polish, and regression coverage for theme settings behavior.
</objective>

<execution_context>
@C:/Users/Peter/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Peter/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-detail-experience-polish/05-CONTEXT.md
@.planning/phases/05-detail-experience-polish/05-RESEARCH.md

<interfaces>
From `app/src/main/java/com/peter/overtimecalculator/domain/Models.kt`:
```kotlin
enum class AppTheme {
    SYSTEM,
    LIGHT,
    DARK,
}
```

From `app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt`:
```kotlin
internal fun SettingsTopBar(title: String, onBack: () -> Unit)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace oversized theme preview cards with a compact always-visible 3-choice control</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeModeSections.kt, app/src/main/java/com/peter/overtimecalculator/ui/settings/ThemeSettingsScreen.kt, app/src/androidTest/java/com/peter/overtimecalculator/MainFlowTest.kt</files>
  <behavior>
    - Test 1: Theme settings still show all three choices with the existing `theme_mode_*` tags.
    - Test 2: The compact control no longer depends on horizontal scrolling to reveal any choice.
    - Test 3: Selecting a choice still surfaces the `*_selected` confirmation path used by current tests.
  </behavior>
  <action>Replace the large theme cards with a compact three-up single-choice control (Material 3 segmented buttons or an equivalent always-visible compact row). Keep all three choices visible simultaneously, preserve the existing `theme_mode_light`, `theme_mode_dark`, `theme_mode_system`, and `*_selected` tags to minimize test churn, and trim surrounding spacing in `ThemeSettingsScreen.kt` only as needed to avoid leaving the old oversized-card gaps behind.</action>
  <verify>
    <automated>./gradlew.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.peter.overtimecalculator.MainFlowTest</automated>
  </verify>
  <done>Theme mode switching is compact, immediately tappable, and fully visible without sideways scrolling.</done>
</task>

<task type="auto">
  <name>Task 2: Harmonize settings top-bar and status-bar chrome through shared edge-to-edge theming</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt, app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt, app/src/main/java/com/peter/overtimecalculator/MainActivity.kt, app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt</files>
  <action>Fix the settings status-bar mismatch in shared settings chrome, not by patching individual screens. Align the settings top-bar container with the surface that should visually occupy the status-bar area, and if needed add centralized system-bar appearance handling in theme/activity code so light/dark icon contrast remains correct under edge-to-edge. Do not disturb the already-good home screen result, and do not introduce one-off per-screen color hacks.</action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug && ./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemeDefaultsTest"</automated>
  </verify>
  <done>All settings routes inherit a more coordinated top/status-bar look while keeping the rest of the app stable.</done>
</task>

</tasks>

<verification>
- [ ] Theme settings androidTest still passes after the compact control swap.
- [ ] Debug build still assembles after shared system-bar/theme changes.
- [ ] Theme defaults regression tests remain green after the chrome harmony update.
</verification>

<success_criteria>
1. Users can switch theme mode without horizontal scrolling.
2. The settings status-bar/top-bar area feels visually integrated with the settings screen.
3. Home and other non-settings surfaces do not regress while settings chrome is harmonized.
</success_criteria>

<output>
After completion, create `.planning/phases/05-detail-experience-polish/05-03-SUMMARY.md`
</output>
