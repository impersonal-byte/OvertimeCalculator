---
phase: 05-detail-experience-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/ui/DayEditorSheet.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/components/CenteredDurationSlider.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/components/DurationSliderMapping.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt
  - app/src/test/java/com/peter/overtimecalculator/CenteredDurationSliderMappingTest.kt
  - app/src/test/java/com/peter/overtimecalculator/CenteredDurationSliderTest.kt
  - app/src/androidTest/java/com/peter/overtimecalculator/DayEditorSliderTest.kt
  - app/src/androidTest/java/com/peter/overtimecalculator/DataManagementBackupRestoreTest.kt
autonomous: true
requirements:
  - UXP-01
  - UXP-04
must_haves:
  truths:
    - Workday editing uses a tighter positive range that makes half-hour input easier to target
    - Workday comp-time editing keeps its negative range intact
    - Existing out-of-range workday records are handled deliberately instead of silently destroyed
    - Backup, restore, and CSV export actions look visually coordinated while keeping distinct behavior
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/ui/DayEditorSheet.kt
      provides: Workday-focused slider range integration in the day editor
    - path: app/src/main/java/com/peter/overtimecalculator/ui/components/DurationSliderMapping.kt
      provides: Tick/range mapping that matches the tighter workday range
    - path: app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt
      provides: Coordinated data-management action button styling
    - path: app/src/androidTest/java/com/peter/overtimecalculator/DayEditorSliderTest.kt
      provides: Regression coverage for workday slider behavior
  key_links:
    - from: app/src/main/java/com/peter/overtimecalculator/ui/DayEditorSheet.kt
      to: app/src/main/java/com/peter/overtimecalculator/ui/components/DurationSliderMapping.kt
      via: effective day type drives min/max range and tick presentation
      pattern: workday uses focused positive range without breaking comp-time editing
    - from: app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt
      to: app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt
      via: shared semantic button colors/emphasis
      pattern: backup/restore/export actions read as one coherent family
---

<objective>
Tighten day-entry precision and align data-management actions with the rest of the settings UI.

Purpose: remove two high-friction polish issues that directly affect daily entry confidence and settings cohesion after the Phase 04 visual migration.

Output: workday-focused slider behavior plus coordinated backup/restore/export button styling, backed by regression tests.
</objective>

<execution_context>
@C:/Users/Peter/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Peter/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-detail-experience-polish/05-CONTEXT.md
@.planning/phases/05-detail-experience-polish/05-RESEARCH.md

<interfaces>
From `app/src/main/java/com/peter/overtimecalculator/ui/DayEditorSheet.kt`:
```kotlin
CenteredDurationSlider(
    valueMinutes = totalMinutes,
    onValueMinutesChange = { totalMinutes = it },
    minMinutes = minMinutes,
    maxMinutes = OvertimeEntryValidator.MAX_OVERTIME_MINUTES,
    centeredVisual = minMinutes < 0,
)
```

From `app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt`:
```kotlin
Button(colors = ButtonDefaults.buttonColors(containerColor = defaults.accent))
OutlinedButton(border = BorderStroke(1.dp, defaults.outline))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rework workday slider range and tick mapping for precise 6-hour editing</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/DayEditorSheet.kt, app/src/main/java/com/peter/overtimecalculator/ui/components/CenteredDurationSlider.kt, app/src/main/java/com/peter/overtimecalculator/ui/components/DurationSliderMapping.kt, app/src/test/java/com/peter/overtimecalculator/CenteredDurationSliderMappingTest.kt, app/src/test/java/com/peter/overtimecalculator/CenteredDurationSliderTest.kt, app/src/androidTest/java/com/peter/overtimecalculator/DayEditorSliderTest.kt</files>
  <behavior>
    - Test 1: Workday editing uses a 6-hour-focused positive range while keeping negative comp-time support.
    - Test 2: Non-workday editing does not regress into an invalid negative range.
    - Test 3: Tick labels remain ordered and readable after the range change.
    - Test 4: Existing workday entries above 6h are handled deliberately rather than silently collapsed on open.
  </behavior>
  <action>Update the range logic, mapping helpers, and visible major ticks together. Do not just lower one constant: the research/oracle review already flagged the hard-coded wide-range tick anchors as a hidden bug source. Implement the user's 6-hour-focused workday editing request while preserving the existing signed-minute save flow and negative comp-time semantics. Handle pre-existing workday values above the focused range explicitly (for example, by temporarily extending the editor upper bound to the current value until the user edits) so opening the sheet does not silently destroy legacy data.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.CenteredDurationSliderMappingTest" --tests "com.peter.overtimecalculator.CenteredDurationSliderTest" && ./gradlew.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.peter.overtimecalculator.DayEditorSliderTest</automated>
  </verify>
  <done>Workday entry feels tighter and more precise, tick math stays coherent, and the editor still respects valid comp-time + legacy-value behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align backup, restore, and CSV export action emphasis in data management</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt, app/src/androidTest/java/com/peter/overtimecalculator/DataManagementBackupRestoreTest.kt</files>
  <action>Replace the lone restore outlined style with a coordinated settings-action hierarchy so backup, restore, and CSV export read as siblings. Keep labels, tags, and behavior wiring unchanged; this task is about visual emphasis alignment, not changing the flow. Prefer semantic Material 3 button colors already supported by the Phase 04 theme defaults instead of introducing ad-hoc hardcoded colors.</action>
  <verify>
    <automated>./gradlew.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.peter.overtimecalculator.DataManagementBackupRestoreTest</automated>
  </verify>
  <done>Data-management actions keep their separate meanings but no longer look like unrelated button systems.</done>
</task>

</tasks>

<verification>
- [ ] Slider mapping/unit tests pass for the tighter workday range.
- [ ] Day-editor androidTest still passes with the new workday behavior.
- [ ] Data-management UI test still passes after button-style alignment.
</verification>

<success_criteria>
1. Workday slider interaction feels more precise without breaking signed-minute saves.
2. Legacy >6h workday entries are handled explicitly instead of being silently lost.
3. Backup, restore, and CSV export actions look intentionally coordinated.
</success_criteria>

<output>
After completion, create `.planning/phases/05-detail-experience-polish/05-02-SUMMARY.md`
</output>
