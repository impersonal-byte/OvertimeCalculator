---
phase: 03-data-backup-and-restore
plan: 04
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementScreen.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/Screens.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/OvertimeAppEffects.kt
  - app/src/test/java/com/peter/overtimecalculator/BackupRestoreViewModelTest.kt
  - app/src/androidTest/java/com/peter/overtimecalculator/DataManagementBackupRestoreTest.kt
autonomous: true
requirements:
  - DATA-02
  - DATA-04
must_haves:
  truths:
    - Users can create a full backup from the data-management area
    - Users can choose a backup file to restore through an explicit in-app flow
    - UI copy makes it clear that CSV export is a share/export tool, not the full restore path
    - Restore rejects CSV or unsupported files before destructive processing begins
    - Restore success and failure are visible to users
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt
      provides: Separate cards/actions for full backup, restore, and CSV export
    - path: app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt
      provides: Backup/restore UI orchestration commands and events
    - path: app/src/main/java/com/peter/overtimecalculator/ui/OvertimeAppEffects.kt
      provides: SAF/document side effects for backup creation and restore file picking
    - path: app/src/test/java/com/peter/overtimecalculator/BackupRestoreViewModelTest.kt
      provides: ViewModel preview/confirm/reject coverage for backup and restore orchestration
    - path: app/src/androidTest/java/com/peter/overtimecalculator/DataManagementBackupRestoreTest.kt
      provides: UI coverage for backup/restore entry points and CSV separation
  key_links:
    - from: DataManagement UI
      to: OvertimeViewModel
      via: explicit onBackup/onRestore/onCsvExport callbacks
      pattern: three separate user actions, not one overloaded export path
    - from: OvertimeAppEffects
      to: SAF / contentResolver
      via: backup file create + restore file open flows
      pattern: explicit document-picking side effects
---

<objective>
Ship the user-facing backup/restore workflow in settings while keeping CSV export clearly separate.

Purpose: Turn the new persistence engine into a real product capability users can discover, trust, and verify from the existing data-management entry point.

Output: Data-management UI, ViewModel/effect wiring, and androidTest coverage for backup/restore flows.
</objective>

<execution_context>
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-02-PLAN.md
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-03-PLAN.md
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementScreen.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/ui/OvertimeAppEffects.kt
</execution_context>

<context>
The existing data-management screen only exposes one action:
```kotlin
SettingCard(
    title = "导出本月数据",
    subtitle = "将当前月的加班与调休明细导出为 CSV 表格，并通过系统分享发送。",
)
```

Phase 3 should expand this into three clearly distinct actions: full backup, restore from backup, and CSV export/share.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand data-management UI into backup, restore, and CSV sections</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementScreen.kt, app/src/main/java/com/peter/overtimecalculator/ui/settings/DataManagementSections.kt, app/src/main/java/com/peter/overtimecalculator/ui/Screens.kt</files>
  <action>Create separate settings actions for full backup and restore, and rewrite the existing CSV section copy so it is clearly framed as a share/export convenience feature rather than a restorable backup. Keep all entry points inside the existing data-management screen and plumb the new callbacks through `Screens.kt` cleanly. Surface the dedicated backup file naming expectations from `03-01` so users can distinguish backups from CSV exports.</action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <done>Data-management UI exposes distinct backup, restore, and CSV export actions with non-ambiguous copy.</done>
</task>

<task type="auto">
  <name>Task 2: Wire backup and restore orchestration through ViewModel and app effects</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt, app/src/main/java/com/peter/overtimecalculator/ui/OvertimeAppEffects.kt</files>
  <action>Add explicit ViewModel events and commands for creating a backup file and choosing a backup file to restore. Route file-system interaction through app effects using SAF/document-based flows rather than hard-coded file paths. Include explicit event/result plumbing for backup-create and restore-open actions so selected `Uri` values return to the ViewModel safely. Run restore through the new `previewRestore()` path first, reject CSV or unsupported files before any write, then require confirmation before destructive restore is applied.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupRestoreViewModelTest"</automated>
  </verify>
  <done>Backup/restore actions are orchestrated through the app's existing ViewModel/effect pattern with visible outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit and androidTest coverage for backup/restore UI flow</name>
  <files>app/src/test/java/com/peter/overtimecalculator/BackupRestoreViewModelTest.kt, app/src/androidTest/java/com/peter/overtimecalculator/DataManagementBackupRestoreTest.kt</files>
  <behavior>
    - Test 1: Data management shows separate backup, restore, and CSV actions.
    - Test 2: CSV action wording remains distinct from backup wording.
    - Test 3: Restore entry path is reachable from settings and surfaces confirmation/success messaging.
    - Test 4: Selecting a CSV or unsupported file produces a safe rejection message before restore begins.
    - Test 5: SAF/document-result plumbing returns a provider-backed `Uri` into the restore preview path.
  </behavior>
  <action>Model the new data-management flow after existing settings/androidTest patterns. Add focused ViewModel tests for preview/confirm/reject logic and androidTest coverage for the new entry points plus provider-backed SAF result handling. Use tags/assertions that protect against future regressions collapsing backup and CSV back into one action or accepting a CSV file as a restore backup.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupRestoreViewModelTest" && ./gradlew.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.peter.overtimecalculator.DataManagementBackupRestoreTest</automated>
  </verify>
  <done>Android UI tests guard the user-facing backup/restore flow and CSV separation.</done>
</task>

</tasks>

<verification>
- [ ] Debug build assembles.
- [ ] Data management has separate backup, restore, and CSV actions.
- [ ] ViewModel/effects support explicit backup + restore flows.
- [ ] Android UI tests cover the new flow.
</verification>

<success_criteria>
1. Users can discover backup and restore from the existing data-management area.
2. Restore is explicit, validated, and visible.
3. CSV export remains available but clearly separate from full backup/restore.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-backup-and-restore/03-04-SUMMARY.md`
</output>
