---
phase: 03-data-backup-and-restore
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/data/backup/BackupRestoreRepository.kt
  - app/src/main/java/com/peter/overtimecalculator/data/db/OvertimeDao.kt
  - app/src/main/java/com/peter/overtimecalculator/data/AppContainer.kt
  - app/src/test/java/com/peter/overtimecalculator/BackupRestoreRepositoryTest.kt
autonomous: true
requirements:
  - DATA-01
  - DATA-03
must_haves:
  truths:
    - The app can produce a backup snapshot from authoritative state, not UI-derived month rows
    - Restoring a valid snapshot preserves configs, entries, and overrides together
    - Restore applies exact durable rows rather than accidentally re-deriving future months differently
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/data/backup/BackupRestoreRepository.kt
      provides: Export and restore service for full-fidelity snapshots
      exports:
        - exportSnapshot
        - previewRestore
        - restoreSnapshot
    - path: app/src/main/java/com/peter/overtimecalculator/data/db/OvertimeDao.kt
      provides: Bulk read/replace primitives needed for transactional restore
    - path: app/src/test/java/com/peter/overtimecalculator/BackupRestoreRepositoryTest.kt
      provides: Round-trip and restore-semantics verification
  key_links:
    - from: BackupRestoreRepository
      to: OvertimeDao
      via: Bulk entity snapshot read and replace operations
      pattern: export/restore touches monthly_config + overtime_entry + holiday_override together
---

<objective>
Implement the persistence-layer backup and restore engine behind the new snapshot contract.

Purpose: Give Phase 3 a safe, testable data boundary that can export and restore authoritative state without routing restore through lossy CSV or UI-only state.

Output: A tested backup/restore repository wired through the app container.
</objective>

<execution_context>
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-01-PLAN.md
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-RESEARCH.md
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/data/AppContainer.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/domain/ConfigPropagationPlanner.kt
</execution_context>

<context>
<interfaces>
From `app/src/main/java/com/peter/overtimecalculator/data/db/OvertimeDao.kt`:
```kotlin
suspend fun getAllConfigs(): List<MonthlyConfigEntity>
suspend fun getEntriesInRange(startDate: String, endDate: String): List<OvertimeEntryEntity>
suspend fun getOverridesInRange(startDate: String, endDate: String): List<HolidayOverrideEntity>
suspend fun upsertConfigs(entities: List<MonthlyConfigEntity>)
```

From `app/src/main/java/com/peter/overtimecalculator/domain/ConfigPropagationPlanner.kt`:
```kotlin
fun plan(allConfigs: List<MonthlyConfig>, updatedSelected: MonthlyConfig): List<MonthlyConfig>
```
</interfaces>

Restore must preserve exact durable rows so future-month configuration behavior stays trustworthy after import. Do not implement restore by replaying current write use cases month by month.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bulk snapshot read and replace primitives with tests</name>
  <files>app/src/main/java/com/peter/overtimecalculator/data/db/OvertimeDao.kt, app/src/test/java/com/peter/overtimecalculator/BackupRestoreRepositoryTest.kt</files>
  <behavior>
    - Test 1: Export path reads all configs, all entries, and all overrides needed for a full snapshot.
    - Test 2: Replace restore clears and replaces durable tables in a controlled order.
    - Test 3: Multi-month configs remain identical after round-trip restore.
  </behavior>
  <action>Extend `OvertimeDao` with the minimum bulk read and replace helpers needed for full snapshot export and restore. Write the repository test first so the contract is explicit. Keep this path authoritative-row based; do not re-trigger propagation logic as if the user re-entered data manually.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupRestoreRepositoryTest"</automated>
  </verify>
  <done>DAO surface can snapshot and replace all durable business rows required by backup/restore.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backup/restore repository and wire it from AppContainer</name>
  <files>app/src/main/java/com/peter/overtimecalculator/data/backup/BackupRestoreRepository.kt, app/src/main/java/com/peter/overtimecalculator/data/AppContainer.kt, app/src/test/java/com/peter/overtimecalculator/BackupRestoreRepositoryTest.kt</files>
  <behavior>
    - Test 1: `exportSnapshot()` returns a payload covering Room truth only.
    - Test 2: `previewRestore()` parses and reports version/scope/incompatibility details without writing.
    - Test 3: `restoreSnapshot()` applies only validated snapshots and preserves exact durable rows.
    - Test 4: Restore excludes `UpdateSessionStore` and holiday remote-cache metadata.
  </behavior>
  <action>Create a dedicated data-layer backup/restore repository that composes the snapshot codec and Room boundary. Wire it through `AppContainer` so later UI can call a single service. Add a non-destructive `previewRestore()` step that parses the selected snapshot and returns confirmation-ready details before any write occurs. Keep the destructive restore transaction focused on durable business truth only; do not mix appearance preferences into the destructive restore transaction. Exclude update-session state and holiday cache from the restorable contract unless the tests prove they are required for user-authored fidelity.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupRestoreRepositoryTest"</automated>
  </verify>
  <done>AppContainer exposes a backup/restore service with passing round-trip tests.</done>
</task>

</tasks>

<verification>
- [ ] `BackupRestoreRepositoryTest` passes.
- [ ] Backup export reads authoritative rows.
- [ ] Restore writes authoritative rows together.
- [ ] Restore does not depend on CSV generation or manual replay of write use cases.
</verification>

<success_criteria>
1. The app has a dedicated persistence-layer backup/restore service.
2. Full-fidelity export/restore round-trips the real business state.
3. Restore semantics preserve month configuration behavior after import.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-backup-and-restore/03-02-SUMMARY.md`
</output>
