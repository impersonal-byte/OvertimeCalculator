---
phase: 03-data-backup-and-restore
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/xml/backup_rules.xml
  - app/src/main/res/xml/data_extraction_rules.xml
  - docs/storage-boundaries.md
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - Manual backup/restore does not silently fight with broad Android auto-restore behavior
    - Volatile non-user state is not treated as part of the user-facing restore contract
    - The repo documents which state is included in manual backup vs excluded as cache/session state
  artifacts:
    - path: app/src/main/res/xml/backup_rules.xml
      provides: Explicit backup inclusion/exclusion rules
    - path: app/src/main/res/xml/data_extraction_rules.xml
      provides: Explicit device-transfer/cloud-backup scope alignment
    - path: docs/storage-boundaries.md
      provides: Restorable-state vs excluded-state documentation
  key_links:
    - from: AndroidManifest.xml
      to: backup_rules.xml / data_extraction_rules.xml
      via: allowBackup + backup configuration references
      pattern: custom backup rules intentionally narrow platform overlap
---

<objective>
Align Android platform backup behavior with the new manual backup/restore feature.

Purpose: Prevent duplicate or confusing restores by narrowing platform backup scope, excluding volatile state, and documenting the exact boundary between user-authored state and cache/session state.

Output: Updated Android backup rules and storage-boundary documentation.
</objective>

<execution_context>
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-RESEARCH.md
@D:/Android.calculator/docs/storage-boundaries.md
@D:/Android.calculator/app/src/main/AndroidManifest.xml
</execution_context>

<context>
The repo currently enables broad backup/device transfer with no meaningful exclusions. This plan must align with the concrete manual restore contract finalized in `03-02`, so it should execute only after the data-layer snapshot boundary is real.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow Android backup rules to durable user state</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/res/xml/backup_rules.xml, app/src/main/res/xml/data_extraction_rules.xml</files>
  <action>Replace the effectively-open backup configuration with explicit include/exclude rules that match the manual backup/restore contract. Keep durable user-authored business state in scope as needed, but exclude volatile update-session state and other non-user state that would produce confusing post-restore behavior. Preserve manifest references to the XML rules and keep the app buildable.</action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <done>Platform backup rules no longer implicitly treat every preference/cache/session file as safe to restore.</done>
</task>

<task type="auto">
  <name>Task 2: Document manual backup scope and exclusions</name>
  <files>docs/storage-boundaries.md</files>
  <action>Update the storage-boundary document so future work can see exactly which state Phase 3 treats as manual-backup truth, which state remains CSV-only export, and which state is intentionally excluded from restore. Call out the rationale for excluding update-session state and cache-like holiday metadata.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('docs/storage-boundaries.md'); text=p.read_text(encoding='utf-8'); assert '备份' in text and '恢复' in text and 'UpdateSessionStore' in text"</automated>
  </verify>
  <done>Repo docs explain the backup/restore boundary clearly enough that future phases do not re-introduce overlap or stale-state restore bugs.</done>
</task>

</tasks>

<verification>
- [ ] Debug build still assembles.
- [ ] Backup XML is explicit, not wide-open.
- [ ] Storage-boundary doc names included and excluded state.
</verification>

<success_criteria>
1. Android auto-backup overlap is intentionally controlled.
2. The repo documents what manual restore should and should not bring back.
3. Volatile session/cache state is kept out of the user-facing restore contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-backup-and-restore/03-03-SUMMARY.md`
</output>
