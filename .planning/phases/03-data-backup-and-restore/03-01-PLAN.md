---
phase: 03-data-backup-and-restore
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/domain/BackupSnapshot.kt
  - app/src/main/java/com/peter/overtimecalculator/data/backup/BackupSnapshotCodec.kt
  - app/src/test/java/com/peter/overtimecalculator/BackupSnapshotTest.kt
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - Backup output captures durable overtime state, not just the current month CSV view
    - Backup schema is versioned so future restores can validate compatibility before writing
    - Backup payload includes monthly configs, overtime entries, and holiday overrides as the durable business truth
    - Backup file type is explicitly distinguishable from CSV export
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/domain/BackupSnapshot.kt
      provides: Versioned backup payload contract for full-fidelity portability
      exports:
        - BackupSnapshot
        - BackupMonthlyConfig
        - RestorePreview
        - BACKUP_FILE_EXTENSION
        - BACKUP_MIME_TYPE
    - path: app/src/main/java/com/peter/overtimecalculator/data/backup/BackupSnapshotCodec.kt
      provides: Snapshot encode/decode contract and validation entrypoint
    - path: app/src/test/java/com/peter/overtimecalculator/BackupSnapshotTest.kt
      provides: Snapshot round-trip and compatibility tests
  key_links:
    - from: BackupSnapshot
      to: MonthlyConfigEntity / OvertimeEntryEntity / HolidayOverrideEntity
      via: Explicit payload fields, not inferred CSV rows
      pattern: one backup contract spanning all durable business tables
---

<objective>
Define and test the full-fidelity backup snapshot contract before any restore logic is written.

Purpose: Give downstream persistence and UI work a stable, versioned payload that can round-trip the app's real business state instead of overloading the existing CSV export.

Output: A tested backup snapshot model and codec contract.
</objective>

<execution_context>
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-RESEARCH.md
@D:/Android.calculator/.planning/phases/03-data-backup-and-restore/03-VALIDATION.md
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/data/db/Entities.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/data/AppearancePreferencesRepository.kt
</execution_context>

<context>
<interfaces>
From `app/src/main/java/com/peter/overtimecalculator/data/db/Entities.kt`:
```kotlin
data class MonthlyConfigEntity(
    val yearMonth: String,
    val hourlyRate: BigDecimal,
    val rateSource: HourlyRateSource,
    val weekdayRate: BigDecimal,
    val restDayRate: BigDecimal,
    val holidayRate: BigDecimal,
    val lockedByUser: Boolean,
)

data class OvertimeEntryEntity(val date: String, val minutes: Int)
data class HolidayOverrideEntity(val date: String, val dayType: DayType)
```

</interfaces>

The current CSV export is intentionally narrower than these contracts. Phase 3 needs a separate snapshot that can restore authoritative state later without reverse-engineering it from visible month rows. To keep backup-vs-CSV separation enforceable, this plan must also define a dedicated backup extension and MIME type.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing snapshot contract tests</name>
  <files>app/src/test/java/com/peter/overtimecalculator/BackupSnapshotTest.kt</files>
  <behavior>
    - Test 1: Encoding then decoding a snapshot preserves monthly configs, entries, and overrides.
    - Test 2: Snapshot payload includes a schema version and creation metadata.
    - Test 3: Decoding rejects missing or unknown schema versions with a readable error.
    - Test 4: Snapshot contract does not include update-session fields or holiday remote-cache metadata.
    - Test 5: Backup contract exposes a dedicated extension and MIME type that are not `text/csv`.
    - Test 6: Validation produces a `RestorePreview`/result object that can drive confirmation UI before writes.
  </behavior>
  <action>Create `BackupSnapshotTest.kt` first. Use concrete fixtures spanning at least two months so config lineage cannot be reduced to a single-month CSV. Make the tests fail before implementation exists.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupSnapshotTest"</automated>
  </verify>
  <done>Failing tests describe the exact round-trip contract and exclusions for the backup snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement versioned snapshot models and codec</name>
  <files>app/src/main/java/com/peter/overtimecalculator/domain/BackupSnapshot.kt, app/src/main/java/com/peter/overtimecalculator/data/backup/BackupSnapshotCodec.kt</files>
  <behavior>
    - Test 1: Valid payloads decode into a typed `BackupSnapshot` object.
    - Test 2: Encoding output is deterministic enough for test assertions on version and section presence.
    - Test 3: Codec refuses unsupported versions instead of silently accepting malformed input.
  </behavior>
  <action>Create a versioned snapshot contract that mirrors durable business state only (`monthly_config`, `overtime_entry`, `holiday_override`). Define explicit backup file constants such as a dedicated extension and MIME type so the later restore picker can reject CSV files early. Include a parsed `RestorePreview`/validation result contract that surfaces version, scope, and compatibility information before any destructive write. Implement a codec that reads and writes this structured snapshot format for SAF-backed files. Do not reuse `createCsvFile()` or CSV column logic; this contract is for full-fidelity backup only.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.BackupSnapshotTest"</automated>
  </verify>
  <done>`BackupSnapshot` and `BackupSnapshotCodec` pass the round-trip tests and clearly separate backup snapshots from CSV export.</done>
</task>

</tasks>

<verification>
- [ ] `BackupSnapshotTest` passes.
- [ ] Snapshot payload is versioned.
- [ ] Snapshot includes full durable business state.
- [ ] Snapshot excludes update-session and holiday-cache-only data.
- [ ] Snapshot has a dedicated file type separate from CSV.
</verification>

<success_criteria>
1. The repo has a typed, versioned backup snapshot contract.
2. Backup schema is explicit enough for later restore validation before destructive writes.
3. Snapshot coverage goes beyond the current monthly CSV export.
4. Backup files are mechanically distinguishable from CSV export files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-backup-and-restore/03-01-SUMMARY.md`
</output>
