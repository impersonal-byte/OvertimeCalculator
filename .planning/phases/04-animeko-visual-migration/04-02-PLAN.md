---
phase: 04-animeko-visual-migration
plan: 02
type: tdd
wave: 1
depends_on: [04-01]
files_modified:
  - app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemePaletteSpec.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt
  - app/src/main/java/com/peter/overtimecalculator/data/AppearancePreferencesRepository.kt
  - app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt
  - app/src/test/java/com/peter/overtimecalculator/ThemePaletteSpecTest.kt
  - app/src/test/java/com/peter/overtimecalculator/ThemeOverviewStateTest.kt
  - app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt
autonomous: true
requirements:
  - THEME-FOUNDATION-01
  - THEME-FOUNDATION-02
must_haves:
  truths:
    - Theme foundation uses HCT-driven palette generation instead of hardcoded per-palette light/dark color constants
    - Semantic token access is centralized in a ThemeDefaults-style surface for downstream UI migration waves
    - Dynamic color remains user-controlled and takes precedence only when enabled and platform-supported
    - Dynamic color default value is OFF for new installs while preserving existing user preference behavior
    - Dark palette strategy targets layered deep-gray surfaces rather than pure black
  artifacts:
    - path: app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemePaletteSpec.kt
      provides: HCT-based palette generation and seed-color mapping contract
    - path: app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt
      provides: semantic surface/container/card tier tokens and role wrappers
    - path: app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt
      provides: precedence orchestration for dynamic color vs fixed HCT palette
    - path: app/src/main/java/com/peter/overtimecalculator/data/AppearancePreferencesRepository.kt
      provides: dynamic-color default OFF persistence baseline
    - path: app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt
      provides: semantic token invariants and dark-layer checks
  key_links:
    - from: OvertimeCalculatorTheme
      to: ThemePaletteSpecs + ThemeDefaults
      via: fixed-color fallback and semantic token locals
      pattern: centralized theme composition before feature-level migration
---

<objective>
Build Wave 1 theme foundation by introducing HCT-driven palette generation and centralized semantic theme defaults.

Purpose: create a stable, reusable visual system so shell/home/editor/settings migrations in later waves can replace legacy styling without ad-hoc per-screen color decisions.

Output: tested theme foundation with dynamic-color precedence, semantic token surface, and backward-compatible settings plumbing.
</objective>

<execution_context>
@D:/Android.calculator/.planning/phases/04-animeko-visual-migration/04-CONTEXT.md
@D:/Android.calculator/.planning/phases/04-animeko-visual-migration/04-PLAN.md
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemePaletteSpec.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/data/AppearancePreferencesRepository.kt
@D:/Android.calculator/app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt
@D:/Android.calculator/app/src/test/java/com/peter/overtimecalculator/ThemePaletteSpecTest.kt
@D:/Android.calculator/app/src/test/java/com/peter/overtimecalculator/ThemeOverviewStateTest.kt
</execution_context>

<context>
Current implementation facts to preserve while migrating:

1. `Theme.kt` currently applies dynamic color first when `dynamicColor == true` and API >= 31, otherwise uses static `ThemePaletteSpecs.fromSeedColor(seedColor)`.
2. `ThemePaletteSpec.kt` currently defines hardcoded color constants for each seed color and composes `lightColorScheme`/`darkColorScheme` directly.
3. `AppearancePreferencesRepository.loadUseDynamicColor()` currently defaults to `true`, and `AppUiState.empty()` also defaults `useDynamicColor = true`.
4. Existing tests assert:
   - seed color coverage in `ThemePaletteSpecTest`
   - dynamic/fixed label behavior in `ThemeOverviewStateTest`

Wave 1 must migrate architecture, not user-facing business behaviors.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce HCT palette generation contract and update palette tests</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemePaletteSpec.kt, app/src/test/java/com/peter/overtimecalculator/ThemePaletteSpecTest.kt</files>
  <behavior>
    - Test 1: All `SeedColor` entries still map to one palette spec.
    - Test 2: Generated light/dark schemes expose non-equal background/surface layers in dark mode.
    - Test 3: Palette preview accents remain stable and non-empty for settings preview usage.
    - Test 4: Dark palette does not collapse to pure black (`0xFF000000`) background by default.
  </behavior>
  <action>Refactor `ThemePaletteSpec.kt` from hardcoded per-palette `lightColorScheme/darkColorScheme` literals to an HCT-driven generation path. Keep `ThemePaletteSpec` API shape compatible enough for existing settings and tests, but ensure generated schemes model layered deep-gray dark surfaces. Update `ThemePaletteSpecTest` to assert generation invariants instead of fixed literal colors.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemePaletteSpecTest"</automated>
  </verify>
  <done>Theme palette generation is HCT-based, test-covered, and still fully seed-color addressable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add centralized semantic ThemeDefaults surface with token invariants</name>
  <files>app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt, app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt, app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt</files>
  <behavior>
    - Test 1: Semantic defaults expose tiered tokens for page/container/card/navigation roles.
    - Test 2: Tokens are derivable from active `ColorScheme` and remain non-transparent for required surfaces.
    - Test 3: Dark-theme semantic tiers preserve visible layer depth (container/card != page background).
    - Test 4: Semantic defaults can be consumed via a single centralized access point.
  </behavior>
  <action>Create `ThemeDefaults.kt` to provide semantic role tokens (page, container, card, accent, state overlays) inspired by animeko structure but implemented clean-room. Wire this through `Theme.kt` so `OvertimeCalculatorTheme` installs semantic locals/wrappers alongside Material `colorScheme`. Do not change typography configuration in this wave.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemeDefaultsTest"</automated>
  </verify>
  <done>A centralized semantic theme layer exists and is validated by unit tests.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce dynamic-color default OFF while preserving precedence and compatibility behavior</name>
  <files>app/src/main/java/com/peter/overtimecalculator/data/AppearancePreferencesRepository.kt, app/src/main/java/com/peter/overtimecalculator/ui/OvertimeViewModel.kt, app/src/test/java/com/peter/overtimecalculator/ThemeOverviewStateTest.kt, app/src/main/java/com/peter/overtimecalculator/ui/theme/Theme.kt</files>
  <action>Set repository dynamic-color default to OFF for first-run baseline (`loadUseDynamicColor()` fallback). Keep existing user toggle behavior and dynamic-color precedence logic in `Theme.kt` unchanged (dynamic wins only when enabled and supported). Ensure overview-state tests still describe correct labels when dynamic is off/on and supported/unsupported. If needed, adjust `AppUiState.empty()` default to match repository baseline so previews/tests do not drift.</action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemeOverviewStateTest"</automated>
  </verify>
  <done>Dynamic-color baseline and precedence match phase decisions with no regression in settings logic.</done>
</task>

</tasks>

<verification>
- [ ] `./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemePaletteSpecTest"` passes.
- [ ] `./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemeDefaultsTest"` passes.
- [ ] `./gradlew.bat :app:testDebugUnitTest --tests "com.peter.overtimecalculator.ThemeOverviewStateTest"` passes.
- [ ] `./gradlew.bat :app:assembleDebug` passes.
- [ ] Theme foundation exposes semantic tokens without introducing ad-hoc per-feature color access.
</verification>

<success_criteria>
1. Theme palette generation is HCT-based and compatible with existing seed-color settings.
2. `ThemeDefaults` semantic tokens are available for downstream wave migration.
3. Dynamic color defaults OFF and still takes precedence when enabled/supported.
4. Dark theme uses layered deep-gray strategy (not pure black) and is test-asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-animeko-visual-migration/04-02-SUMMARY.md`.
</output>
