# 04.1-02 Summary - 亮色层级色卡重构

## Outcome

已按真机反馈完成 4.1 第二轮：从边框补丁切换为语义分层色卡方案。亮色卡片通过层级色差建立结构，不再依赖黑色描边。

## What Changed

### 1) 亮色 token 层级重构
- 更新 `app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt`。
- 亮色分层改为基于 `primaryContainer` 的 tier 混色：
  - `navigationContainer`、`sectionContainer`、`cardContainer`、`cardElevatedContainer`。
- 保持暗色分支原有策略，避免 dark 视觉回归。

### 2) 回退边框补丁，恢复色卡层级表达
- 更新 `app/src/main/java/com/peter/overtimecalculator/ui/HomeSummarySections.kt`。
- 更新 `app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt`。
- 更新 `app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsMainSections.kt`。
- 移除 04.1-01 临时添加的边框修复，改由层级色差承担卡片分组感。

### 3) 回归测试增强
- 更新 `app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt`。
- 增加亮色层级差异断言，防止 token 回退导致卡片再次“贴平背景”。

## Verification Evidence

Passed on 2026-03-21:
- `.\gradlew.bat :app:assembleDebug --stacktrace`
- `.\gradlew.bat :app:testDebugUnitTest --stacktrace`

## Pending

- 真机亮色复测（用户侧）：
  - 首页 Summary/月份切换卡层级是否自然；
  - 设置主页与设置分组卡是否有清晰分层；
  - 暗色模式是否无明显副作用。
