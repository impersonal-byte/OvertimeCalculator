# 04.1-03 Summary - Light Theme Tonal Layer Reinforcement

## Outcome

Executed a third 4.1 iteration to make card hierarchy in light mode visibly layered by tonal color steps (not border-only contrast).

## What Changed

1) Light token separation was increased in `ThemeDefaults`:
- `navigationContainer`, `sectionContainer`, `cardContainer`, and `cardElevatedContainer` now use wider tonal spacing in light mode.
- Dark-mode branch was kept unchanged.

2) Core cards now use semantic layered containers plus elevation:
- Home summary card gradient and elevation tuned in `HomeSummarySections.kt`.
- Settings content card switched to semantic card container in `SettingsCommon.kt`.
- Settings category navigation cards switched to elevated semantic card container in `SettingsMainSections.kt`.

3) Guardrail tests were strengthened:
- Added luminance-delta assertions in `ThemeDefaultsTest.kt` to prevent future flattening of light-theme layers.

## Verification Evidence (2026-03-21)

Passed:
- `./gradlew.bat :app:assembleDebug --stacktrace`
- `./gradlew.bat :app:testDebugUnitTest --stacktrace`

Environment used:
- `JAVA_HOME=C:\Program Files\Android\Android Studio\jbr`
- `GRADLE_USER_HOME=D:\Android.calculator\.gradle-user`

## Pending

- Real-device confirmation in light mode:
  - Home summary card has clear layered separation from page background.
  - Month switcher + settings cards show consistent hierarchy.
  - Dark mode remains visually stable.
