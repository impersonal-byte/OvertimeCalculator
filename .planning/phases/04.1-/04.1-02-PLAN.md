---
phase: 04.1-light-theme-hierarchy-fix
type: plan
status: executed
depends_on:
  - 04.1-01
---

## Objective

根据真机反馈，将 04.1 修复从“边框补丁”重构为“亮色语义分层色卡”方案，确保卡片通过层级色差而非黑色描边建立视觉结构。

## Scope

- `app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt`
- `app/src/main/java/com/peter/overtimecalculator/ui/HomeSummarySections.kt`
- `app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsCommon.kt`
- `app/src/main/java/com/peter/overtimecalculator/ui/settings/SettingsMainSections.kt`
- `app/src/test/java/com/peter/overtimecalculator/ThemeDefaultsTest.kt`

## Tasks

1. 调整亮色下 `ThemeDefaults` 的 navigation/section/card/elevated 分层色，采用 token 混色提升层次感。
2. 移除 04.1-01 中新增的边框补丁（SummaryCard、MonthSwitcher、SettingCard、CategoryNavigationCard）。
3. 增加主题分层回归断言，确保亮色层级不会退化到“贴平背景”。
4. 通过 `assembleDebug` 与 `testDebugUnitTest` 门禁。

## Verification Gate

- `.\gradlew.bat :app:assembleDebug --stacktrace`
- `.\gradlew.bat :app:testDebugUnitTest --stacktrace`

## Notes

- 真机亮色截图是最终验收依据；暗色模式只允许轻微视觉波动，禁止结构回归。
