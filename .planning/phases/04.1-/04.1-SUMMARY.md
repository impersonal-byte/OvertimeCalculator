# 04.1 Summary - 亮色卡片层次修复（迭代中）

## Outcome

4.1 已完成两轮迭代：  
- 04.1-01：边框增强（初版）  
- 04.1-02：改为语义分层色卡（当前主方案，替代 04.1-01 的边框补丁）

## What Changed

### 1) 亮色语义 token 调整
- 更新 `app/src/main/java/com/peter/overtimecalculator/ui/theme/ThemeDefaults.kt`。
- 对亮色模式引入更明确的分层混色策略（navigation/section/card/elevated）。
- 提升 `outline` 在亮色模式下的可见性，改善边界感。

### 2) 语义分层色卡方案（当前主方案）
- 以 `ThemeDefaults` 的亮色 tier 混色重建层级（navigation/section/card/elevated）。
- 回退边框补丁，改由容器色差体现结构层次。

## Verification Evidence

Passed on 2026-03-21:
- `.\gradlew.bat :app:assembleDebug --stacktrace`
- `.\gradlew.bat :app:testDebugUnitTest --stacktrace`

## Pending

- 真机亮色模式复测（用户侧）：
  - 首页 Summary 卡、月份切换卡是否明显分层且无黑边修复感；
  - 设置主页/设置分组卡是否不再“贴平背景”；
  - 暗色模式无视觉回归。
